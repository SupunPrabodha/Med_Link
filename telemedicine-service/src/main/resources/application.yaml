server:
  port: 8087

spring:
  application:
    name: telemedicine-service
  datasource:
    url: jdbc:postgresql://localhost:5437/telemedicinedb
    username: telemedicine
    password: telemedicine
  jpa:
    hibernate:
      ddl-auto: update
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka

app:
  jitsi-base-url: https://meet.jit.si
  doctor-base-url: http://localhost:8084

management:
  endpoints:
    web:
      exposure:
        include: health,info
